import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.globe.observerlite',
  appName: 'GLOBE Observer Lite',
  webDir: 'dist'
};

export default config;
